# Supabase Configuration
# IMPORTANT: These credentials must match the Hub app exactly
# All apps (Hub, Trainer, Chef) share the same Supabase project

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (safe for client-side use)
VITE_SUPABASE_ANON_KEY=eyJhbGci...

# Hub URL for SSO origin validation
# PRODUCTION:
VITE_HUB_URL=https://fitcopilot.app

# LOCAL DEVELOPMENT (uncomment for local testing):
# VITE_HUB_URL=http://localhost:5175

# Gemini API Key (for recipe generation)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ========================================
# 🔒 SECURITY: JWT Secret NOT NEEDED!
# ========================================
# 
# ❌ DO NOT ADD: VITE_SUPABASE_JWT_SECRET
#
# WHY NOT?
# - JWT secret would be exposed in client-side code (security risk!)
# - VITE_ prefix makes it visible in browser bundles
# - Chef uses server-side validation via Supabase auth API
# - JWT secret exists ONLY in Edge Function (server-side)
#
# HOW IT WORKS:
# 1. Hub generates tokens via Edge Function (with JWT secret)
# 2. Hub sends access_token + refresh_token to Chef
# 3. Chef calls supabase.auth.setSession() with these tokens
# 4. Supabase validates tokens SERVER-SIDE (no client verification!)
# 5. User authenticated securely without exposing secrets
#
# For more info, see: docs/sso-architecture/SSO_ARCHITECTURE.MD
# ========================================

# Development Notes:
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to git (it's in .gitignore)
# 3. Restart dev server after changing environment variables
# 4. All apps must use the same VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
# 5. For local dev, uncomment the localhost Hub URL above
