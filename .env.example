# Supabase Configuration
# IMPORTANT: These credentials must match the Hub app exactly
# All apps (Hub, Trainer, Chef) share the same Supabase project

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (safe for client-side use)
VITE_SUPABASE_ANON_KEY=eyJhbGci...

# JWT secret for SSO token verification (shared with Hub app)
# This must match the Hub's Legacy JWT Secret exactly
# Found in: Supabase Dashboard → Settings → API → JWT Settings
VITE_SUPABASE_JWT_SECRET=your_jwt_secret_here

# Hub URL (for CORS and SSO postMessage origin validation)
VITE_HUB_URL=http://localhost:5175

# Gemini API Key (for recipe generation)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Development Notes:
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to git (it's in .gitignore)
# 3. Restart dev server after changing environment variables
# 4. All apps must use the same VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
# 5. The VITE_SUPABASE_JWT_SECRET must match across Hub, Trainer, and Chef
